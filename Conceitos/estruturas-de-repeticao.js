/* while / for / foreach */

let voltasAteCansar = 5
let qtdVoltaAtual = 0

while( qtdVoltaAtual < voltasAteCansar ) {
    console.log(`Dar uma volta na quadra`)
    qtdVoltaAtual++
}

console.log(`-----------------------------------`)

// for

const qtdCalculada = 3

for(let qtdPetiscos = 1; qtdPetiscos <= qtdCalculada; qtdPetiscos++ ) {
    console.log(`Dando o petisco de nº ${qtdPetiscos}`)
}

const gatos = ['Lessie', 'Pony', 'Fumaça']

for (let indice = 0; indice < gatos.length; indice++) {
    console.log(`Dando Petisco de nº ${fgatos[indice]}`)
}